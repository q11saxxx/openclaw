# 本文件说明：容器化部署入口，后续统一编排前端、后端与数据库。
version: '3.9'
services:
  backend:
    build:
      context: ../backend
    ports:
      - '8000:8000'
  frontend:
    build:
      context: ../frontend
    ports:
      - '5173:5173'
